version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
      - SECRET_KEY=${SECRET_KEY:-change-me-in-production}
      - JWT_SECRET_KEY=${JWT_SECRET_KEY:-${SECRET_KEY:-change-me-in-production}}
      - DATABASE_URL=${DATABASE_URL:-sqlite:///instance/bluprint.db}
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
      - CORS_ORIGINS=${CORS_ORIGINS:-}
    volumes:
      - ./instance:/app/instance
      - ./logs:/var/log/bluprint
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s

  # Optional: PostgreSQL database (uncomment if using PostgreSQL)
  # db:
  #   image: postgres:15-alpine
  #   environment:
  #     - POSTGRES_USER=bluprint
  #     - POSTGRES_PASSWORD=${DB_PASSWORD:-change-me}
  #     - POSTGRES_DB=bluprint
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   restart: unless-stopped

# volumes:
#   postgres_data:

